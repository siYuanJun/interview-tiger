# 方案设计文档

## 项目信息
- 项目名称: interview-tiger
- 需求来源: [模块5-对话体验优化需求文档.md](file:///Users/siyuan/Documents/www/ai-project/interview-tiger/.ai-workflow/meta-agent-collaboration/docs/模块5-对话体验优化需求文档.md)
- 创建时间: 2026-07-06
- 状态: 进行中

---

## R1: 滚动体验优化

### 方案对比

| 方案 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| 方案A: 独立滚动容器 | 简单直接，用户体验好 | 内容过长时仍需滚动 | ⭐⭐⭐ |
| 方案B: 折叠展开 | 节省空间，一目了然 | 需要额外交互 | ⭐⭐⭐⭐ |
| 方案C: 两者结合 | 最优体验 | 实现稍复杂 | ⭐⭐⭐⭐⭐ |

### 推荐方案：方案C（独立滚动 + 折叠展开）

**设计思路**：
1. AI气泡内容超过200px高度时，自动启用独立滚动
2. 添加折叠/展开按钮，用户可手动控制显示高度
3. 默认展开前3行内容，点击展开显示全部

**技术实现**：
```css
.dialogue-bubble-ai-content {
  max-height: 200px;
  overflow-y: auto;
  transition: max-height 0.3s ease;
}
.dialogue-bubble-ai-content.expanded {
  max-height: none;
}
```

**修改文件**：
- `frontend/src/components/DialogueItem.vue` — 添加折叠按钮和滚动容器
- `frontend/src/assets/main.css` — 添加滚动样式和动画

---

## R2: UI结构化展示

### 方案对比

| 方案 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| 方案A: 标题+内容分区 | 清晰直观，符合用户预期 | 需要修改模板结构 | ⭐⭐⭐⭐⭐ |
| 方案B: 卡片式布局 | 视觉层次分明 | 改动较大 | ⭐⭐⭐⭐ |
| 方案C: Markdown渲染 | 支持丰富格式 | 需要引入Markdown库 | ⭐⭐⭐ |

### 推荐方案：方案A（标题+内容分区）

**设计思路**：
1. 在AI气泡内区分"问题标题"和"回答内容"两个区域
2. **标题区域**：背景高亮、字体加粗、字号稍大，显示面试官问题
3. **内容区域**：普通文本样式，显示大模型回答，支持换行和列表

**UI设计**：
```
┌─────────────────────────────────────────────────────┐
│  🤖 AI建议                                          │
├─────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────┐    │
│  │ 📝 问题：你好能听到我说话吗                    │    │
│  │    （背景：primary/10，边框：primary/20）      │    │
│  └─────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────┐    │
│  │ ✅ 可以听到，请开始提问。                     │    │
│  │    （普通文本，支持列表）                      │    │
│  └─────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────┘
```

**技术实现**：
- 修改 `DialogueItem.vue`，添加标题区域和内容区域
- 修改 `main.css`，添加标题区域的样式

---

## R3: Prompt优化

### 方案对比

| 方案 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| 方案A: 修改模板约束 | 简单直接，效果明显 | 需要测试调整 | ⭐⭐⭐⭐⭐ |
| 方案B: 添加问题分类 | 根据问题类型定制回复 | 需要分类逻辑 | ⭐⭐⭐⭐ |
| 方案C: 两者结合 | 最优效果 | 实现稍复杂 | ⭐⭐⭐⭐⭐ |

### 推荐方案：方案C（模板约束 + 问题分类）

**设计思路**：
1. 修改Prompt模板，添加严格约束：
   - 禁止客套话和废话
   - 确认类问题：回答不超过3句话
   - 技术问题：结构化、重点突出
2. 添加问题分类逻辑，根据问题类型调整回复风格

**修改后的Prompt模板**：
```
你是一位资深的面试辅导专家。请根据以下信息，为求职者生成一个针对面试官话语的回应建议。

【面试官话语】
{question}

【回答要求】
1. 判断问题类型：确认类/技术类/行为类/其他
2. 确认类问题（如"能听到吗"、"准备好了吗"）：简短回答，1-2句话即可，直接回应
3. 技术类问题：结构化回答，采用STAR法则（情境-任务-行动-结果），重点突出
4. 行为类问题：结合实际经验，给出具体例子
5. 禁止客套话、废话、开场白（如"好的，针对..."、"这是一个专业问题"等）
6. 直接输出回答内容，不要包含分析、说明、注意事项等额外内容
7. 回答简洁直接，控制在50-200字

请直接生成回应建议：
```

**问题分类规则**：
| 类型 | 判断规则 | 示例 |
|------|---------|------|
| 确认类 | 包含"能"、"可以"、"好"、"准备"等词 | "能听到吗？"、"准备好了吗？" |
| 技术类 | 包含技术名词、概念、框架等 | "介绍一下RAG"、"什么是微服务？" |
| 行为类 | 包含"你做过"、"经历"、"处理"等词 | "你遇到过什么挑战？" |
| 其他 | 不属于以上类型 | 其他问题 |

**修改文件**：
- `backend/app/services/prompt.py` — 修改Prompt模板
- `backend/app/services/question_judge.py` — 添加问题分类逻辑（可选）

---

## R4: 交互流畅度优化

### 方案对比

| 方案 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| 方案A: 自动重启监听 | 简单直接，效果明显 | 需要处理状态管理 | ⭐⭐⭐⭐⭐ |
| 方案B: 并行处理 | 真正的并行，体验最好 | 实现复杂 | ⭐⭐⭐⭐ |
| 方案C: 延迟重启 | 简单，避免频繁切换 | 有延迟感 | ⭐⭐⭐ |

### 推荐方案：方案A（自动重启监听）

**设计思路**：
1. 修改 `forceStop()` 函数，改为 `pauseRecognition()`，保留识别状态
2. 在处理完语音结果后，自动重启语音监听
3. 确保大模型生成和语音识别并行执行

**状态流程**：
```
listening → recognizing → pause → process → restart listening
                                              ↓
                                        大模型生成（并行）
```

**技术实现**：
- 修改 `useSpeech.ts`，添加 `pauseRecognition()` 和 `resumeListening()` 方法
- 修改 `InterviewPage.vue`，在 `handleSpeechResult` 后自动重启监听

**修改文件**：
- `frontend/src/composables/useSpeech.ts` — 添加暂停/恢复方法
- `frontend/src/components/InterviewPage.vue` — 修改处理流程

---

## 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                     InterviewPage.vue                           │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    对话列表容器                          │   │
│  │  ┌─────────────┐    ┌─────────────────────────────┐    │   │
│  │  │ 用户消息     │    │ AI气泡 (独立滚动+折叠)        │    │   │
│  │  │             │    │ ┌──────────────────────┐     │    │   │
│  │  │ 你好能听到我 │    │ │ 📝 问题：你好能听到我吗 │     │    │   │
│  │  │ 说话吗？     │    │ └──────────────────────┘     │    │   │
│  │  └─────────────┘    │ ┌──────────────────────┐     │    │   │
│  │                     │ │ ✅ 可以听到，请开始提问 │     │    │   │
│  │                     │ │    (滚动内容区)        │     │    │   │
│  │                     │ └──────────────────────┘     │    │   │
│  │                     │ [折叠/展开]                 │    │   │
│  │                     └─────────────────────────────┘    │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    语音识别状态                          │   │
│  │  useSpeech.ts ──────► handleSpeechResult ──────► 重启   │   │
│  │       │                       │                        │   │
│  │       ▼                       ▼                        │   │
│  │   语音监听              提交文本 → 调用大模型(并行)      │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                         Backend                                │
│  ┌─────────────────┐    ┌─────────────────┐                   │
│  │  prompt.py      │───►│  llm.py         │                   │
│  │  (优化模板)     │    │  (调用大模型)   │                   │
│  └─────────────────┘    └─────────────────┘                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 实施顺序

| 序号 | 任务 | 依赖 | 预估时间 |
|------|------|------|---------|
| 1 | R4 交互流畅度优化 | 无 | 0.5天 |
| 2 | R3 Prompt优化 | 无 | 0.5天 |
| 3 | R2 UI结构化展示 | 无 | 0.5天 |
| 4 | R1 滚动体验优化 | R2 | 0.5天 |
| 5 | 联调测试 | 全部 | 0.5天 |

---

## 风险评估

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|---------|
| Prompt优化效果不佳 | 大模型回复仍啰嗦 | 中 | 多轮测试调整模板 |
| 语音识别重启失败 | 用户无法继续说话 | 低 | 添加错误处理和重试机制 |
| 折叠展开动画卡顿 | 用户体验差 | 低 | 使用CSS动画，避免JavaScript操作 |

---

## 质量门控

- [ ] 方案经用户确认
- [ ] 技术方案与代码库一致
- [ ] 无架构冲突
- [ ] 风险评估完成
