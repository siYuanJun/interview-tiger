# 2026-07-02 面试虎项目工作日志

## 已完成工作

### 1. 密钥配置 + Docker容器化
- `backend/.env` 写入真实密钥（ARK_API_KEY, KB_API_KEY）
- `backend/Dockerfile` 国产镜像源 python:3.12-slim
- `docker-compose.yml` 桥接网络 172.30.90.0/24，静态IP 172.30.90.10，端口映射 8001:8000
- 后端 Docker 启动，前端 Mac 宿主机直接启动

### 2. 联网搜索降级功能
- `backend/app/services/llm.py` 新增 `enable_search` 参数，支持 Bot 端点和 `enable_search` 参数两种方式
- `backend/app/services/prompt.py` 新增联网搜索专用 Prompt 模板
- `backend/app/routes/question.py` 知识库无结果 → 自动 `use_web_search=True`
- `backend/config.py` 新增 `WEB_SEARCH_BOT_ID`

### 3. 语音识别引擎升级（ASR）
- 调研确认火山引擎有「豆包语音·大模型流式语音识别」开放API，与豆包输入法同引擎
- `backend/app/services/asr.py` — ASR WebSocket客户端（自定义二进制帧协议）
- `backend/app/routes/asr.py` — FastAPI WebSocket 代理 `/api/asr/stream`
- `frontend/src/composables/useVolcanoASR.ts` — AudioContext PCM → base64 → 后端 WebSocket
- `frontend/src/composables/useSpeech.ts` — 双引擎：火山ASR优先 + Web Speech降级
- `requirements.txt` 新增 `websocket-client==1.8.0`
- 产出需求文档：`docs/模块1-语音识别引擎升级需求文档.md`
- PRD文档已同步更新所有ASR相关章节

### 4. 配置集中化
- 后端 `config.py` 唯一真实来源（ARK_MODEL, KB_ID, WEB_SEARCH_BOT_ID, ASR_APP_ID等）
- 前端 `constants.ts` 唯一真实来源（DEFAULT_MODEL_ID, DEFAULT_KB_ID）
