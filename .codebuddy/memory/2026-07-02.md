# 2026-07-02 工作日志

## 面试虎项目初始化与完整开发

按照 ai-dev-workflow 技能工程化流程，从0到1完成了「面试虎 - AI智能面试助手」全栈项目。

### 完成内容

**Step 1-3：项目初始化 + PRD转化 + 任务拆解**
- 创建 `.ai-workflow/` 工程化目录
- `workflow-state.json` — 技术栈：Vue 3 + FastAPI + 火山引擎
- `tech-requirements.md` — 18个功能点映射到技术实现
- `INDEX.md` — 21个任务 + Mermaid依赖图

**Step 4：任务执行（21/21 全部完成）**

后端（9个任务）：
- FastAPI入口 + CORS + 全局异常处理
- 配置管理 API（POST/GET /api/config）
- 健康检查 API（GET /api/health）
- 知识库检索服务（volcengine SignerV4签名，支持rerank过滤）
- 大模型调用服务（流式SSE + 非流式双模式）
- Prompt拼接服务（知识库增强 / 降级双模板）
- 问题处理路由（/api/question + /api/question/stream，全部流程串联）
- 知识库检索路由 + 大模型生成路由

前端（12个任务）：
- Vue 3 + Vite + Tailwind CSS + Pinia + TypeScript 完整骨架
- HomePage首页（Logo + 开始面试 + 浏览器兼容检测）
- ConfigModal配置弹窗（大模型Key/知识库ID/AK:SK/模型ID）
- useRecorder录音Composable（MediaRecorder API）
- useSpeech语音识别Composable（Web Speech API，自动重连）
- useApi API调用Composable（Axios + SSE流式fetch）
- InterviewPage面试主页面（左右分栏对话 + 状态指示 + 结束确认）
- DialogueItem对话展示组件（打字机效果 + 知识库增强标记）
- questionJudge问题判断工具（4重规则 + 相似度去重）
- Pinia面试状态管理（对话/配置/导出）
- 响应式布局（PC左右/移动端上下）
- 对话记录导出（JSON/TXT/Markdown）

### 验证结果
- 前端 vite build：95 modules ✅
- 后端路由注册：8个API路由 ✅
- 后端API测试：参数校验/健康检查全部通过 ✅

### 技术要点
- 火山引擎LLM：`https://ark.cn-beijing.volces.com/api/v3/chat/completions`，Bearer Token认证
- 火山引擎知识库：`https://api-knowledgebase.mlp.cn-beijing.volces.com/api/knowledge/collection/search_knowledge`，SignerV4签名
- 知识库结果过滤：rerank_score < 0.3 丢弃
- 知识库为空时自动降级为无知识库模式
- 流式输出使用SSE，前端用 ReadableStream + fetch 解析

### Git 安全审计与首次提交 (15:00)
- 两轮全项目隐私扫描，修复了 ConfigModal.vue 泄露真实密钥前缀的问题
- 删除 `backend/.env.dev`（含模型名称），新建 `backend/.env.example`（通用占位符）
- `.gitignore` 加固：屏蔽 `docs/字节跳动接口调用指南.md`、`backend/.env*`、`dist/`、`node_modules/`
- 首次提交：`9a76363 feat: 面试虎 - AI智能面试助手 v1.0.0`，76 files, 6335 insertions
- 安全验证：`git ls-files` 确认无 `.env`/`node_modules`/`dist/`/`__pycache__` 被追踪
