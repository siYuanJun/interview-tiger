# 2026-07-10

## RAG 3.0 升级实施（meta-feature-dev 工作流）

### 背景
- 使用 meta-feature-dev 7 阶段工作流，对 interview-tiger 实施 RAG 从 2.5 → 3.0 升级（路径A）
- 完成 Stages 1-5（规划阶段），用户批准后进入 Stage 6（执行）

### 新增文件
- `backend/app/services/hybrid_retriever.py` — BM25+Dense 混合检索，RRF 融合，EnsembleRetriever
- `backend/app/services/query_rewriter.py` — 15 个面试领域同义词映射，最多生成 3 个查询变体
- `backend/app/services/memory.py` — 会话级内存缓存，TTL 过期 + 最大会话数淘汰，追问模式检测
- `backend/app/services/validator.py` — LLM 批量内容校验，3 秒超时兜底，YES/NO 解析

### 修改文件
- `backend/app/services/local_knowledge.py` — 集成 4 个 RAG3 模块，懒加载初始化，全管道搜素
- `backend/config.py` — 新增 8 个 RAG3 配置变量
- `backend/app/routes/question.py` — 新增 session_id 字段支持
- `backend/requirements.txt` — 添加 jieba==0.42.1, rank_bm25==0.2.2, pydantic>=2.6.1
- `backend/Dockerfile` — site-packages 快照优先解压，跳过网络 pip install

### Docker 测试 & 修复
- jieba 实际已在镜像中（Requirement already satisfied），未造成额外下载
- **pydantic 2.6.1 + Python 3.12 兼容性 Bug**：langchain 导入触发 `ForwardRef._evaluate()` 错误 → 升级到 pydantic 2.13.4
- **rank_bm25 缺失**：BM25 回退纯向量检索 → `pip install rank_bm25==0.2.2`
- **site-packages 全量备份**：导出 2.8G 压缩包到 `backend/site-packages.tar.gz`，Dockerfile 优先解压

### Stage 6 验证结果（api-test-harness）
- ✅ 7/7 全通过（4 非流式 + 3 SSE 流式）
- QueryRewriter ✅ | Hybrid BM25=0.3 ✅ | Validator 6→1 ✅ | Memory hit ✅
- 测试产出：`.ai-workflow/api-test/2026-07-10-rag3-upgrade/`

### 待完成
- Stage 7：测试文档 + 变更记录（可选）
- Docker Compose 挂载 pip 缓存卷（`./backend/.pip-cache:/root/.cache/pip`）
